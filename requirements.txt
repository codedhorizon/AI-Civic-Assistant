# =============================================================================
# requirements.txt
# AI Civic Assistant Agent - Indian Pension Scheme Advisor
#
# Install everything with one command:
#   pip install -r requirements.txt
#
# After installing, also install the Tesseract OCR binary
# (needed for scanned PDFs and image documents):
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu  : sudo apt install tesseract-ocr poppler-utils
#   macOS   : brew install tesseract poppler
# =============================================================================

# ── LangChain core ────────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0

# ── Text splitter (split into its own package in LangChain 0.2) ───────────────
langchain-text-splitters>=0.2.0

# ── Ollama: local LLM + local embeddings (no API key needed) ─────────────────
langchain-ollama>=0.1.0

# ── Web search (no API key needed) ───────────────────────────────────────────
duckduckgo-search>=5.3.0

# ── PDF reading and AcroForm filling ─────────────────────────────────────────
pypdf>=4.0.0

# ── Vector store (in-memory, no server needed) ───────────────────────────────
faiss-cpu>=1.7.4

# ── PDF generation ────────────────────────────────────────────────────────────
reportlab>=4.0.0

# ── OCR: for scanned PDFs and image documents ────────────────────────────────
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
